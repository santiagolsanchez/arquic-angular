import { PipeExample1Pipe } from './pipe-example-1.pipe';

describe('PipeExample1Pipe', () => {
  it('create an instance', () => {
    const pipe = new PipeExample1Pipe();
    expect(pipe).toBeTruthy();
  });
});
