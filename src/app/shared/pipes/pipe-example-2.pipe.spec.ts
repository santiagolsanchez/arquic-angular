import { PipeExample2Pipe } from './pipe-example-2.pipe';

describe('PipeExample2Pipe', () => {
  it('create an instance', () => {
    const pipe = new PipeExample2Pipe();
    expect(pipe).toBeTruthy();
  });
});
